/// <reference types="vite/client" />

/**
 * Version de `package.json`, inyectada por Vite en tiempo de build.
 * Ver `define` en `vite.config.ts` y `src/version.ts`.
 */
declare const __APP_VERSION__: string;
